// lib/core/constants/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background     = Color(0xFF0B0B1A);
  static const Color surface        = Color(0xFF16162D);
  static const Color surfaceDeep    = Color(0xFF111128);
  static const Color accent         = Color(0xFF00E5FF);
  static const Color accentGreen    = Color(0xFF69FF47);
  static const Color accentBlue     = Color(0xFF0066FF);

  static const Color textPrimary    = Colors.white;
  static const Color textSecondary  = Colors.white54;
  static const Color textMuted      = Colors.white30;
  static const Color textDim        = Colors.white24;

  static const Color divider        = Colors.white10;
  static const Color border         = Colors.white12;
}
